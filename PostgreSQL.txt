SQL comandos e tags.


//Criando uma table
create table nome_tabela(
    atributo,
    atributo
)
*****
serial
primary key
not null
default
*****


id serial primary key,  // isso seria um id int primary key auto_increment
descricao varchar(100) not null,
status integer default 1, // que de inicio e iniciado com valor 1


//insert
insert to nome_tabela (coluna_tabela) values ('valor_passado')

//Fazendo UPDATE
update nome_tabela set coluna_tabela = value where id = 3

//Fazendo o DELETE
delete from nome_tabela where id = 3

//Fazendo Consulta SELECT
select * from nome_tabela  //mostra tudo
select * from nome_tabela where condição_Tabela_Search

//Ordenando os dados
select * from nome_tabela order by coluna_tabela // ordenando apartir do id , por padrao ta vindo asc (crescente) se por desc (decrescente).


//Operador Or ou where 
select * from nome_tabela where coluna_tabela  = value  or coluna_tabela = value

//Chave estrangeira
foreign key (coluna_tabela) reference tabela_onde_esta_chave_primaria (coluna_tabela_da_relação)

//Fazendo consulta com relacionamento
select * from nome_tabela inner join tabela_chaveEstrangeira on nome_tabela.chave_estrangeira = tabela_chaveEstrangeira.coluna

 